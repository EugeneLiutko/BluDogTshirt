@using EuluBlueMarket.Models
@model LoginModel

@{
    /**/

    ViewBag.Title = "Login";
}

<h2 style="margin-top:40px; margin-left: 15%; margin-right: auto;">Вход на сайт</h2>
@if (User.Identity.IsAuthenticated)
{
    //@Html.RenderAction("Main", "Market")
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div style="padding-left:10px; margin-left: 15%; margin-right: auto;">
        <div style="padding:5px; margin:auto;">
            @Html.ValidationSummary()
            <input type="hidden" name="returnUrl" value="@ViewBag.returnUrl" />
            <p>
                <label>Email</label><br />
                @Html.EditorFor(x => x.Email)
            </p>
            <p>
                <label>Password</label><br />
                @Html.EditorFor(x => x.Password)
            </p>
            <br />
            <p><button type="submit">Увійти</button></p>
        </div>
</div >
                    }
